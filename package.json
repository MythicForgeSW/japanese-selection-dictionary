{
  "name": "japanese-selection-dictionary",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "package": "mkdir -p dist && rm -f dist/japanese-selection-dictionary.zip && zip -r dist/japanese-selection-dictionary.zip manifest.json LICENSE README.md src icons -x '*.DS_Store'"
  }
}
